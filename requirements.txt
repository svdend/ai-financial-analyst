# ── Data & computation ────────────────────────────────────────────────────────
pandas==2.2.3
numpy==1.26.4          # <2.0 required for prophet compatibility
duckdb==1.1.3
statsmodels==0.14.6

# ── Time-series forecasting ───────────────────────────────────────────────────
prophet==1.1.6
cmdstanpy==1.2.4       # prophet's Stan backend; first import downloads ~200 MB cmdstan
statsforecast==1.7.8   # AutoARIMA, AutoETS — replaces pmdarima
numba==0.60.0          # required by statsforecast

# ── ML / feature selection ────────────────────────────────────────────────────
scikit-learn==1.5.2
shap==0.46.0

# ── External data ─────────────────────────────────────────────────────────────
requests==2.32.3
yfinance==1.3.0        # ETF total returns for macro features (notebook 03)
fredapi==0.5.2         # FRED macro data (Prompt 6)

# ── Config & serialisation ────────────────────────────────────────────────────
pyyaml==6.0.2

# ── Excel generation ──────────────────────────────────────────────────────────
openpyxl==3.1.5

# ── Anthropic AI ──────────────────────────────────────────────────────────────
anthropic==0.40.0

# ── Tableau ───────────────────────────────────────────────────────────────────
tableauhyperapi          # intentionally unpinned; platform-specific wheels

# ── Notebook execution ────────────────────────────────────────────────────────
papermill==2.6.0

# ── Testing ───────────────────────────────────────────────────────────────────
pytest==8.3.4
pytest-cov==6.0.0
hypothesis==6.122.3
responses==0.25.3
deepdiff==8.1.1

# ── Code quality ──────────────────────────────────────────────────────────────
ruff==0.8.4
mypy==1.13.0
pandas-stubs==3.0.3.260530
types-PyYAML==6.0.12.20260518
types-requests==2.33.0.20260518
detect-secrets==1.5.0
pre-commit==4.0.1
